---
title: "Unfreeze Account"
description: "This endpoint is used to send a request to unfreeze a customer's account."
api: "POST /account/bank/freeze"
---

# Unfreeze Account

Use this endpoint to unfreeze a customer's account by setting the **freeze** parameter to `false`.

---

## Request Parameters

<ParamField body="accountNumber" type="string" required>
  The account number for which the unfreeze action is to be performed.  
  **Example:** `5118745523`
</ParamField>

<ParamField body="reason" type="string" required>
  The reason for which the unfreeze is requested.  
  **Example:** `Account activity cleared for resumption`
</ParamField>

<ParamField body="freeze" type="boolean" required>
  Indicates the action to be taken on the account:  
  - `true`: Freeze the account.  
  - `false`: Unfreeze the account.  
  **Example:** `false`
</ParamField>

---

<ResponseExample>

```json
{
	"statusCode": 00,
	"message": "Account successdully frozen",
	"data": {},
	"errors": []
}
```

</ResponseExample>
